apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sealed-secret.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - httpproxy.yaml
generatorOptions:
  disableNameSuffixHash: true
