apiVersion: v1
kind: Service
metadata:
  name: htmly
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  selector:
    app: htmly
  ports:
    - name: http
      port: 80
      targetPort: 80
